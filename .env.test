# ══════════════════════════════════════════════════════════════════════
# ⚠️  STAGING / TESTING ONLY — DO NOT USE IN PRODUCTION
# ══════════════════════════════════════════════════════════════════════
# These API keys and base URLs point to the staging environment.
# They are for local development and QA testing ONLY.
# Never use staging keys or staging endpoints in production.
# Production keys must be stored in a secrets manager (not in .env files).
# ══════════════════════════════════════════════════════════════════════

NODE_ENV=test
PORT=3001

# ── External API — OMS (Appointment Details + Challan GET) ─────────
OMS_BASE_URL=https://oms-purchase-stage.qac24svc.dev
OMS_API_KEY=PcLHVSx97orSVxWqIS0yExFwjVP29EY1

# ── External API — Challan Service (POST Create) ───────────────────
CHALLAN_SERVICE_BASE_URL=https://challan-service-stage.qac24svc.dev
CHALLAN_SERVICE_API_KEY=Y2hhbGxhbi1zZXJ2aWNlLXN0YWdl

# ── Infrastructure ─────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379
MONGODB_URI=mongodb://localhost:27017/challan-test

# ── Storage ────────────────────────────────────────────────────────
STORAGE_MODE=local
LOCAL_UPLOAD_DIR=./uploads
# AWS keys not needed for local mode:
AWS_BUCKET=
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# ── Playwright ─────────────────────────────────────────────────────
PLAYWRIGHT_HEADLESS=false      # set true to run in background
WORKER_CONCURRENCY=2
OTP_TIMEOUT_MS=600000          # 10 minutes

# ── Alerting (leave blank for local) ──────────────────────────────
SLACK_WEBHOOK_URL=

# ── Test Vehicle — RJ14CV8337 (from RC card) ───────────────────────
# registrationNumber : RJ14CV8337
# chassisNumber      : MA3ETDE1S00114822  (last4: 4822)
# engineNumber       : K10BN-1730089      (last4: 0089)
TEST_APPOINTMENT_ID=           # fill with a real staging appointmentId for RJ14CV8337
TEST_MOBILE_NUMBER=            # fill with vehicle owner's mobile number
